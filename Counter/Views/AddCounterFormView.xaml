<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Counter.ViewModels"
             x:Class="Counter.Views.AddCounterFormView"
             x:DataType="viewModels:CountersViewModel">
    <Grid RowSpacing="16"
          ColumnSpacing="8"
          Padding="0,12,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Entry Grid.Row="0"
               Grid.ColumnSpan="2"
               Placeholder="Counter Name"
               Text="{Binding NewCounterName}"/>
        <Entry Grid.Row="1"
               Grid.Column="0"
               Placeholder="Initial Value"
               Keyboard="Numeric"
               Text="{Binding NewCounterInitialValue}"/>
        <Entry Grid.Row="1"
               Grid.Column="1"
               Placeholder="Color (e.g., #FF0000)"
               Text="{Binding NewCounterColor}"/>
        <Button Grid.Row="2"
                Grid.ColumnSpan="2"
                Text="Add Counter"
                Command="{Binding AddCounterCommand}"/>
    </Grid>
</ContentView>
